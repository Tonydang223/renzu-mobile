import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const Form = () => {
  return (
    <View>
      <Text> alo </Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Form;
